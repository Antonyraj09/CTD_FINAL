@using CTD_FINAL.Entities
@model JobIsne
@{
    Layout = null;
    var agent = ViewBag.Agent as SubAgent;
    string D(DateTime? d) => d?.ToString("dd.MM.yyyy") ?? "";
    string V(object? v) => v?.ToString() ?? "";
}
<!DOCTYPE html>
<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40">
<head>
    <meta charset="utf-8" />
    <title>CTD Submission Checklist - @Model.JobNumber</title>
    <!--[if gte mso 9]>
    <xml>
        <w:WordDocument>
            <w:View>Print</w:View>
            <w:Zoom>100</w:Zoom>
            <w:DoNotOptimizeForBrowser/>
        </w:WordDocument>
    </xml>
    <![endif]-->
    <style>
        @@page { size: A4; margin: 18mm 16mm; }
        body { font-family: Arial, Helvetica, sans-serif; color: #000; font-size: 11.5px; }
        p { margin: 0 0 6px; font-weight: 700; line-height: 1.5; }
        .hdr { text-align: center; margin-bottom: 4px; }
        .hdr .title { font-size: 14px; font-weight: 700; }
        .hdr .subtitle { font-size: 12px; font-weight: 700; }
        .hdr .note { font-size: 10.5px; font-weight: 700; }
        .divider { border-top: 1px solid #000; margin: 8px 0 10px; }
        .val { color: #FF0000; }
        .footer { margin-top: 16px; font-size: 10px; color: #888; text-align: right; font-weight: 400; }
    </style>
</head>
<body>
    <p>&nbsp;&nbsp;JOB NO: ISNE/<span class="val">@V(Model.JobNumber.Replace("ISNE/", ""))</span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;CTD NO: <span class="val">@V(Model.CtdNumber)</span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;DT: <span class="val">@D(Model.CtdDate)</span></p>

    <div class="hdr">
        <p class="title">ANNEXURE &ndash; A</p>
        <p class="subtitle">DATA SHEET FOR SUBMISSION OF CTD FOR NEPAL TRANSIT CARGO</p>
        <p class="note">(FILL UP USING BLOCK LETTERS)</p>
    </div>
    <div class="divider"></div>

    <p>CHA CODE: <span class="val">@V(agent?.LicenseNo)</span>, CHA NAME: <span class="val">@V(agent?.Name ?? Model.SubAgentName)</span>,</p>
    <p>EXIT CUSTOMS SITE: (PL TICK), ROUTE OF TRANSIT: <span class="val">@V(Model.RouteOfTransit)</span></p>
    <p>IMPORTER'S CODE: <span class="val">@V(Model.ImporterCode)</span></p>
    <p>IMPORTER'S NAME &amp; ADDRESS: <span class="val">@V(Model.PartyName)</span></p>
    <p><span class="val">@V(Model.Address)</span>, PAN: <span class="val">&nbsp;</span>.</p>
    <p>NOTIFY NAME AND ADDRESS: <span class="val">SAME AS ABOVE</span></p>
    <p>INVOICE NO: <span class="val">@V(Model.InvoiceNumber)</span>, DATE: <span class="val">@D(Model.InvoiceDate)</span></p>
    <p>CERTIFICATE OF ORIGIN: <span class="val">@V(Model.CertificateOfOrigin)</span>, DATE: <span class="val">@D(Model.CertificateOfOriginDate)</span></p>
    <p>LC / EL NO: <span class="val">@V(Model.LcNo)</span>,&nbsp; DATE: <span class="val">@D(Model.LcDate)</span>,&nbsp; COUNTRY OF ORIGIN: <span class="val">@V(Model.CountryOrigin)</span></p>
    <p>COUNTRY OF CONSIGNMENT: <span class="val">@V(Model.CountryCgn)</span>.&nbsp; CURRENCY CODE: <span class="val">@V(Model.Currency)</span></p>
    <p>INS. POLICY NO:&nbsp;<span class="val">&nbsp;</span>, DATE:&nbsp;<span class="val">&nbsp;</span>, EXP DATE:&nbsp;<span class="val">&nbsp;</span></p>
    <p>INSURANCE COMPANY NAME &amp; ADD : <span class="val">@V(Model.SensitiveCargo ? Model.InsuranceCompanyNameAddress : null)</span></p>
    <p>IGM NO: <span class="val">@V(Model.RotNo)</span>, IGM DATE: <span class="val">@D(Model.RotDate)</span>, LINE NO: <span class="val">@V(Model.LineNo)</span>, INWARD DATE: <span class="val">@D(Model.InwardDate)</span></p>
    <p>MBL NO: <span class="val">@V(Model.MblNo)</span>, DATE: <span class="val">@D(Model.MblDate)</span>, HBL NO: <span class="val">@V(Model.HblNo)</span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;DATE: <span class="val">@D(Model.HblDate)</span></p>
    <p>
        TOTAL PACKAGE: <span class="val">@(Model.Containers.Sum(c => c.NoPackages) is var totalPkg && totalPkg > 0 ? totalPkg.ToString() : "")</span>,
        PACKAGE CODE: <span class="val">@V(string.Join(", ", Model.Containers.Select(c => c.PackageType).Where(p => !string.IsNullOrWhiteSpace(p)).Distinct()))</span>,
        MARK AND NO: <span class="val">@(Model.Containers.Any(c => !string.IsNullOrWhiteSpace(c.MarksSerial)) ? string.Join(", ", Model.Containers.Select(c => c.MarksSerial).Where(m => !string.IsNullOrWhiteSpace(m))) : "N/M")</span>
    </p>
    <p>
        GROSS WT: <span class="val">@(Model.GrossWeight.HasValue ? Model.GrossWeight.Value.ToString("N3") : "")</span>,
        NET WT: <span class="val">@(Model.NetWeight.HasValue ? Model.NetWeight.Value.ToString("N3") : "")</span>,
        UNIT: <span class="val">KGS</span>
    </p>
    @if (Model.Containers.Any())
    {
        foreach (var c in Model.Containers)
        {
            <p>CONTAINER NO/SHIPPER SEAL NO: <span class="val">@V(c.ContainerNo) &ndash; @V(c.ContainerSize.TrimEnd('t', 'f'))'</span></p>
        }
    }
    else
    {
        <p>CONTAINER NO/SHIPPER SEAL NO: <span class="val">&nbsp;</span></p>
    }
    @{ var itemNo = 0; }
    @foreach (var c in Model.Containers)
    {
        itemNo++;
        <p>ITEM NO: <span class="val">@itemNo</span>, CTH: <span class="val">@V(c.CustomsCode)</span>,&nbsp; SENSITIVE: <span class="val">@(Model.SensitiveCargo ? "YES" : "NO")</span></p>
    }
    @if (!Model.Containers.Any())
    {
        <p>ITEM NO: <span class="val">&nbsp;</span>, CTH: <span class="val">&nbsp;</span>,&nbsp; SENSITIVE: <span class="val">@(Model.SensitiveCargo ? "YES" : "NO")</span></p>
    }
    <p>DESCRIPTION OF GOODS: <span class="val">@V(Model.CargoDescription ?? Model.MiscDescription)</span></p>
    <p>CIF VALUE IN FC: @V(Model.Currency). <span class="val">@(Model.CifFc.HasValue ? Model.CifFc.Value.ToString("N2") : "")</span>,&nbsp; ANTI DUMPING DUTY(INR): <span class="val">&nbsp;</span></p>
    <p>DUTY EXCLUDING ANTI DUMPING AND SAFEGUARD(INR): <span class="val">@(Model.DutyAmount.HasValue ? Model.DutyAmount.Value.ToString("N2") : "")</span></p>
    <p>VHICLE NO: <span class="val">&nbsp;</span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;CHASSIS NO: <span class="val">&nbsp;</span></p>
    <p>ENGINE NO: <span class="val">&nbsp;</span>&nbsp;&nbsp;&nbsp;DATE OF REGN: <span class="val">&nbsp;</span></p>
    <p>MODE OF TRANSPORT &ndash; ROAD</p>

    <p style="margin-top:18px;">FOR <span class="val">@V(agent?.Name ?? Model.SubAgentName)</span></p>
    <p>&nbsp;</p>
    <p>&nbsp;</p>
    <p><span class="val">@V(agent?.ContactPersonName)</span></p>
    <p>AUTHORIZED SIGNATORY</p>
    <p>DT: <span class="val">@D(DateTime.Today)</span></p>

    <p class="footer">Printed from CTD Management System &middot; @DateTime.Now.ToString("g")</p>
</body>
</html>
